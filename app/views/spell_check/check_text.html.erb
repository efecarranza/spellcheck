<h1>Let's check your text!</h1>

<% fixed_string = @response %>

<p class="original">Original text: 
	<%= @response.parsed_response['original'] %>
</p>

<h3>Your text seems to have some words mispelled, here's how we'd fix it.</h3>


<table> 
	<thead>
	<tr>
		<th width="40%">Original Word</th>
		<th width="60%">Suggestions</th>
		<th colspan="3"></th>
	</tr>
	</thead>
<tbody>
<% @response.parsed_response['corrections'].each do |word, suggestions| %>
  <tr>
  	<td><%= word %></td>
  	<td>
  	<% suggestions.each do |suggestion| %>
  		<%= link_to "#{suggestion}", '#', class: "replace-word" %>,
  	<% end %>
  </td>
  </tr>
<% end %>
</tbody>
</table>